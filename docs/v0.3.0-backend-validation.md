# XDRemux Flutter v0.3.0 backend validation

> 本文档记录 `codex/swift-backend-v0.3.0` 的实际验证结果。它取代
> `handoff-macos-ios-20260731.md` 中关于 iOS 尚未创建的旧结论；后续判断以当前代码和本文档为准。

## 基线

- 分支：`codex/swift-backend-v0.3.0`
- 起始提交：`3297c27 chore: start v0.3.0 swift backend line`
- Flutter：`0.3.0+18`
- Rust core：`0.3.0`
- Windows Installer：`0.3.0`
- 验证日期：2026-08-07

## main 同步记录

已读取并同步 `origin/main` 的 v0.2.8 功能性更新（`b3aadae`、`f2d954c`）：移除顶栏 OPPO 快捷开关，将 HDR family、OPPO 兼容、相机尾部和严格 tmap 收入默认折叠的“高级模式”，并保留 v0.2.8 release notes。v0.3.0 的 Flutter/Rust/Installer 版本号未回退，P0.0 Swift backend 选项保留在 Apple 平台设置中。

## P0.0 已完成

- 新增 `ConversionBackend.rust` / `ConversionBackend.swift`；Rust 是默认值。
- 新增统一 request、progress、cancellation、error、output-validation contract。
- Rust adapter 保留原 FFI 参数、进度 handle、后台 isolate 和硬件编码路径。
- macOS/iOS 通过 `xdremux/swift-backend` MethodChannel 暴露 capability probe。
- Swift 选项仅在 macOS/iOS 显示；Windows/Android 不显示。
- Swift capability 在未接入原生包的构建中明确为 unavailable；不会把“可见”误报为“可用”。
- 未添加 Swift CLI 子进程路径；P0.2 仅在 macOS Swift 后端按 capability 开放 Photographic Styles，Apple Portrait 仍关闭。

取消语义目前是 best-effort：Rust FFI 没有中断原语，因此取消中的 Rust 调用会继续运行到 native 返回，Flutter 不会再把其结果标记为“已转换”。Swift 也沿用同一个 request id；上游 Core 当前没有可注入的硬中断点。

## P0.1 已接入（macOS Swift 标准 HDR）

- `apps/flutter/macos/SwiftBackend/Package.swift` 以 exact `1.3.1` 依赖上游 `XDRemuxCore` / `XDRemuxAppleFeatures`，没有合并 upstream 历史。
- `XDRemuxFlutterBackend` 直接调用 `ConversionEngine.convert` 和 `ConversionEngine.isValidOutput`；没有启动 Swift CLI、读取 stdout 或解析 CLI 文本。
- `Runner/AppDelegate.swift` 通过 `xdremux/swift-backend` MethodChannel 调用 Swift Library，并通过 `xdremux/swift-backend/progress` EventChannel 转发统一进度事件。
- Swift capability 当前报告 `swiftStandardHdr=true`、`swiftPhotographicStyles=true`、`swiftPortrait=false`；Photographic Styles 仍显示实验性警告，Portrait 不显示。
- Swift 后端的输出在原生侧先做 ISO gain-map 验证，Dart 侧再执行统一输出验证；失败不会标记为已转换。
- 批次完成时显示实际选择的后端；Swift 转换错误会在队列项和批次提示中明确标记为 Swift 后端错误。
- Swift 后端只在 macOS 接入。iOS 仍保留 capability gating，不链接这个 macOS-only package，也不启动 Swift CLI。
- iOS 的 `xdremux/swift-backend` 当前仅返回明确的 unavailable capability stub；不会伪装成 Swift Core 或 AppleFeatures 已支持。

## 实际环境

- macOS：`macOS 27.0 (26A5388g)`
- Xcode：`27.0 (27A5218g)`，`xcode-select` 指向 `/Applications/Xcode-beta.app/Contents/Developer`
- Swift：`6.4`
- Rust：`rustc 1.97.0` / `cargo 1.97.0`
- Flutter：`3.44.8` / Dart `3.12.2`
- iPhone：无线连接 `Beet的iPhone`，iOS `27.0 (24A5390f)`；`flutter devices` 可发现
- CocoaPods：1.17.0（用户 Ruby gem 环境）
- Android SDK：未安装

## macOS deployment target 评估

在 P0.1 接入前，Flutter macOS 工程的三个层面是 macOS 12：

- `apps/flutter/macos/Podfile`：`platform :osx, '12.0'`
- `apps/flutter/macos/Podfile` 的 `post_install`：Pods target 写入 `MACOSX_DEPLOYMENT_TARGET = '12.0'`
- `apps/flutter/macos/Runner.xcodeproj/project.pbxproj`：Runner Debug/Profile/Release 均为 `MACOSX_DEPLOYMENT_TARGET = 12.0`

上游 `v1.3.1` 的 `Package.swift` 明确声明 `.macOS(.v15)`。macOS 12 不能被视为兼容配置，因此本阶段已将以下配置统一提升到 macOS 15：

- `apps/flutter/macos/Podfile` 的 `platform :osx`；
- `Podfile` 的 `post_install` 生成的 `MACOSX_DEPLOYMENT_TARGET`；
- `Runner.xcodeproj` 的 Debug/Profile/Release 项目级 deployment target。

原生 Package resolve 和 Swift Package 编译已验证通过；CocoaPods 安装后，arm64 Flutter Runner 已完成最终链接；universal 目标仍受上游 x86_64 `Float16` 编译问题阻断。

即使提升到 macOS 15，也不能据此开放 iOS：上游 Core 使用 `Process`/`xcrun`，Portrait 使用外部 `zstd`，AppleFeatures 使用 Vision/CoreImage 及原生辅助资源，需单独做 iOS 编译与运行时 capability 验证。

## P0.1 真实样例验证

使用用户提供的 `/Users/beet/Desktop/src/` 中 5 张 HEIC 样例；原始照片和该目录已有的 `out/` 文件均未修改，也没有复制进仓库。测试机器为 macOS 27.0，Swift 6.4，Xcode 27 beta，且 Swift package 固定为上游 `v1.3.1`。

- Rust：使用 `tests/conformance` 的 Rust 转换路径，输出到临时目录 `/tmp/xdremux-p01-rust/`。
- Swift：使用 `XDRemuxSwiftBackend.convert` 直接调用 `ConversionEngine.convert`，未启动 CLI 子进程，输出到临时目录 `/tmp/xdremux-p01-swift/`。
- 5/5 样例的 Swift 转换均返回 `success=true`，并通过原生 `ConversionEngine.isValidISOGainMapOutput`；Rust 生成的 5/5 输出也通过 conformance dump 的 ISO gain-map 结构检查。

| 输入样例 | Rust 输出 | Swift 输出 | 结果 |
|---|---:|---:|---|
| `IMG20231216104338.heic` | 7,025,767 bytes | 7,094,213 bytes | 两者均有效 |
| `IMG20231216104454.heic` | 3,944,728 bytes | 3,924,302 bytes | 两者均有效 |
| `IMG20240302184556.heic` | 2,190,624 bytes | 2,234,780 bytes | 两者均有效 |
| `IMG20240321181713.heic` | 1,664,063 bytes | 1,762,515 bytes | 两者均有效 |
| `IMG20260711121521.heic` | 9,332,086 bytes | 9,132,765 bytes | 两者均有效 |

这次验证证明的是“同一输入分别由两条后端路径生成有效 ISO HDR HEIC”，不是字节级或元数据完全等价。当前 5 个样例都观察到结构差异：Rust 输出包含 `auxC` 和 `mimehdrgm-xmp`，Swift 上游 `v1.3.1` 输出不包含这两项；Swift 的 `ftyp` 还包含 `miaf`，而 Rust 输出未包含。两者都包含 `tmap`，并通过对应的结构验证，因此该差异记录为后续 Apple Photos 导入验证的关注项，不据此宣称正式稳定支持。

样例输入与输出的 SHA-256 已记录如下，便于复测：

| 样例 | 输入 SHA-256 | Rust 输出 SHA-256 | Swift 输出 SHA-256 |
|---|---|---|---|
| `IMG20231216104338.heic` | `fb2179f78c0632a393798a6df4a1ca2e995e34ba0f8a7226a2addb1190bc21e5` | `70624419748a4b4a3daa2410aa4630a77c869b2216ad35a025f1150da3b844bc` | `1e6ba808e9b74752c03bf86576e7832a9b6f14980412022b18ae73b7f31fa7aa` |
| `IMG20231216104454.heic` | `06237d9721b679f8db01dcdfe25d65f1ae3ab5416c078cdd6743cc0083257e41` | `e7144f5067d8b15cfb0fff810bb55c55781644a757eb0e01dc62e1fa850f9d29` | `0a54d36f08777256fc310f17c3c7d58c25ccb924cb1acc9555a8535e61124517` |
| `IMG20240302184556.heic` | `894e5ce44ab2d909e0f00735c1cdafaa790ac3fc3bd99dba9a7e42bf600cf5bd` | `0862cb57631334ef379c5892f1efcb1955d0caf50851942f65ae6f55aac89bd5` | `14ed61915f10ddd33ff80d6d824fd9c3740dbb0c487265022bc2d7de3b5d1ba9` |
| `IMG20240321181713.heic` | `4aa4f57c60f699652d09829fb6660d918787810569d74b8f441545bb76255079` | `7f7581d2cb25663729468fbd4f5823639351206eb21a660b9015d36d29ebd716` | `cfabe3b3ffc272184602221b9b485ea03c2b76dee57973f30bcfc5fee83216d7` |
| `IMG20260711121521.heic` | `654ee45c8523ff49cce22e978f2002a81a59c32de034ed965ac849a7792c475d` | `c806fe32527b067d34562ac40a574566b1de953276ec9635c37989f0226c51e0` | `08729435a7c8230fcd9d9f3055309bc6a376a03c183831db693dc5566f8ab378` |

## P0.2 AppleFeatures 接入状态

已将上游 `XDRemuxAppleFeatures` SwiftPM product 接入 macOS bridge，并让统一请求携带 `applePhotographicStyles` / `applePortrait`。原生转换在 Apple 功能请求下直接调用 `AppleFeatureConversionEngine`，随后使用上游 Apple 输出验证器，而不是用 Rust validator 代替；OPPO-compatible 请求仍由上游和 Flutter 双侧拒绝/清除。

当前 Photographic Styles capability 已通过一个版本化 resource compatibility overlay 开放，但仍保留实验性警告和单文件失败处理。Overlay 将上游 helper 对旧的 `initWithURL:type:image:useEmbeddedPreview:` 调用适配到当前系统提供的 `initWithURL:type:useEmbeddedPreview:`，只写入 app cache，不修改 SwiftPM checkout。

首次整组运行曾为 4/5，`IMG20240302184556.heic` 在 Vision semantic helper 阶段失败；随后该样本被单独复现并成功，最后重新执行完整 5 张样例，5/5 均生成输出并通过 `AppleFeatureConversionEngine.isValidOutput`。5 个输出均包含 `tmap`、`styleMetadata` 和 Apple semantic auxiliary graph；这证明当前 host 上的兼容层和本次样本验证可用，但不构成长期稳定性或 Apple Photos 正式兼容声明。

完整复测输出位于临时目录 `/tmp/xdremux-p02-styles-rerun-20260807/`，原始样例未修改：

| 输入样例 | Styles 输出 | SHA-256 |
|---|---:|---|
| `IMG20231216104338.heic` | 7,609,288 bytes | `8c973788995f259e4b3b29a5d41aeae09d167f0953d106100e645b6c5f4258a2` |
| `IMG20231216104454.heic` | 4,411,461 bytes | `564c89696efae4041da161c9f3ab2031423a1f2f8f323d1faff4ed13112e1f0d` |
| `IMG20240302184556.heic` | 2,539,433 bytes | `95ec40e0e67a506564a9e5f26c131c367044c9d7dedd8b1e52938c357e537d08` |
| `IMG20240321181713.heic` | 1,928,680 bytes | `342cfb73330c0f5bc70720e62540c661f6360e90c8bc9cdb1f89c7b96bd011e4` |
| `IMG20260711121521.heic` | 2,684,889 bytes | `8bb213599509734d4092c35850c16fb96c8150cef5bf34fe8f68f2296b1b279a` |

只读 runtime 检查确认当前系统提供的是 `initWithURL:type:useEmbeddedPreview:`；兼容层只在检测到该 selector 且上游 resource 文本形状匹配时启用，否则 capability 自动关闭。

Apple Portrait 本机也没有开放：上游通过 `/usr/bin/env zstd` 解码 portrait payload，本机没有 `/usr/bin/zstd`（仅有 Homebrew 路径），而 iOS 不能启动该外部进程。Portrait 仍需真正的可转换人像输入、zstd 路径策略和输出 manifest/辅助图验证后再显示。

## 验证结果

| 检查 | 结果 | 备注 |
|---|---|---|
| `cargo check --workspace --locked` | 通过 | Rust workspace 正常 |
| `cargo build --release -p xdremux-core` | 通过 | 生成 arm64 `libxdremux_core.dylib` |
| macOS dylib symbol/load smoke test | 通过 | `flutter test test/ffi_smoke_test.dart`，版本返回 `0.3.0` |
| `flutter pub get` | 通过 | 依赖已解析；依赖版本未改变，lockfile hosted URL 保持项目原有镜像配置 |
| `flutter analyze` | 通过（仅 info） | 10 条既有/信息级 lint，无 error |
| `flutter test` | 通过 | 12 tests passed，含 backend model/UI gating tests |
| `flutter build macos --release` | 阻断（universal） | CocoaPods 1.17.0 已安装并完成 `pod install`；当前 Xcode 27 beta 的 x86_64 编译路径在上游 `XDRemuxCore/RAW/CoreImageRAW.swift` 的 `Float16` API 报错，arm64 路径未受影响 |
| `flutter devices` | 通过 | macOS、Chrome、无线 iPhone 均可发现 |
| `flutter build ios --debug --no-codesign` | 通过 | 生成 `build/ios/iphoneos/Runner.app`，Xcode 38.3 秒；产物 Mach-O arm64、最低 iOS 15.0；验证的是现有 Rust 路径，未链接 Swift Core |
| `swift package resolve && swift build` | 通过 | `apps/flutter/macos/SwiftBackend`，同时编译 `XDRemuxCore` 与 `XDRemuxAppleFeatures`，上游 `XDRemux` v1.3.1，macOS 15 |
| `swift test` | 通过（默认 gate） | capability contract 通过；P0.1/P0.2 外部样例测试仅在显式设置环境变量时运行 |
| P0.2 Apple Styles 真实样例 smoke test | 通过（本次复测） | 5/5 样例通过 Apple 输出验证；完整外部样例测试耗时 433.098 秒 |
| `xcodebuild -resolvePackageDependencies` | 通过 | Runner 正确解析本地 `XDRemuxFlutterBackend` 与上游 `XDRemuxCore` |
| Xcode 原生编译阶段 | 通过 | `AppDelegate.swift`、Swift bridge 和 `XDRemuxCore` 编译到 macOS 15 target |
| Xcode arm64 完整链接 | 通过 | `xcodebuild -workspace Runner.xcworkspace -scheme Runner -configuration Release -arch arm64 -sdk macosx CODE_SIGNING_ALLOWED=NO build`；产物最低 macOS 15.0，Mach-O arm64 |

## 未宣称的能力

当前 P0.1 已完成 macOS 原生 Swift Library 接入，以及同一批真实样例的双后端有效输出验证；现有 iOS Rust 路径也已完成 debug device build，但 iOS Swift Core 尚未嵌入，Apple 功能没有开放。仍未宣称“Swift/Rust 输出等价”，也未完成 Apple Photos 导入流程。P0.2 的 Photographic Styles bridge、兼容层和结构验证已接入并按实验性能力开放；Portrait 仍因 zstd/真实输入验证不足而关闭。当前 arm64 Runner 已完成最终链接；universal 构建仍需处理上游 `XDRemuxCore` 在当前 Xcode beta x86_64 路径的 `Float16` 兼容性，之后再记录 Photos 导入、编辑、保存、退出和重新打开结果。

已读取上游 `21Z121Z1/XDRemux` 的 `v1.3.1` tag（commit `6d99b568`）：其 `Package.swift` 当前只声明 `.macOS(.v15)`，并将 `XDRemuxCore`、`XDRemuxAppleFeatures` 和 CLI 分成 SwiftPM targets。本阶段只通过固定版本 SwiftPM 依赖接入这两个 Library product，没有把上游独立历史整体合并，也没有将 macOS-only 源码误标为 iOS 可用。
