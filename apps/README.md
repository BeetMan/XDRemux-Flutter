# Apps

Graphical or platform-specific application shells live here.

The conversion CLIs remain under `xdremux/`; app projects should depend on or mirror converter behavior deliberately instead of sharing an ambiguous source directory with the CLI entry points.
