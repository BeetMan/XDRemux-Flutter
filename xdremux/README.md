# XDRemux Converter Entrypoints

This directory contains converter entry points and reusable converter-side implementation.

- `swift-cli/` is the recommended macOS Swift CLI path.
- `python/` is the cross-platform Python CLI path.

Graphical app shells are intentionally kept outside this directory under `apps/`.
