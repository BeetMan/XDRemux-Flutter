# macOS Apps

`XDRemuxApp/` contains the macOS SwiftUI shell for XDRemux.

Keep macOS-specific Xcode projects, assets, and app-level tests here. The Swift command-line converter lives separately under `xdremux/swift-cli/`.
