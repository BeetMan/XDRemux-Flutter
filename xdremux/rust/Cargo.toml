[package]
name = "xdremux-core"
version = "0.1.0"
edition = "2021"
authors = ["BeetMan"]
license = "MIT"
description = "XDRemux cross-platform HEIC conversion core (Rust)"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
