# XDRemux v1.2

Prepared from the current main branch after v1.1.

## Highlights

- Keeps the Swift CLI as the recommended macOS conversion entry point.
- Moves the Swift CLI from `xdremux/swift/XDRemux.swift` to `xdremux/swift-cli/XDRemux.swift`.
- Moves the macOS SwiftUI app from `xdremux/swift/XDRemuxApp` to `apps/macos/XDRemuxApp` so the CLI and GUI shell are not mixed in one source directory.
- Documents the current hybrid input-processing branch as the production default.
- Preserves the Python CLI as the cross-platform entry point.
- Clarifies OPPO compatibility behavior: clean Apple/ImageIO-compatible output by default, with `--oppo-compat` reserved for OPPO Gallery recognition signals and private compatibility data.
- Standardizes repository layout documentation and keeps reusable local automation in `scripts/`.

## User impact

Users should invoke the main converters through `xdremux/swift-cli/XDRemux.swift` or `xdremux/python/XDRemux.py`.

The macOS app helper script remains `scripts/build_and_run.sh`, but it now targets the app project under `apps/macos/XDRemuxApp`.

## Validation status

This release-preparation pass inspected the repository metadata, README entry points, current release state, and the checked-in helper script. A full macOS ImageIO/Xcode validation run was not performed in this Linux container because the Swift CLI imports Apple-only frameworks.

## Release checklist

- Create tag `v1.2`.
- Create GitHub Release title `XDRemux v1.2`.
- Use this file as the release body after merging the repository layout PR.
