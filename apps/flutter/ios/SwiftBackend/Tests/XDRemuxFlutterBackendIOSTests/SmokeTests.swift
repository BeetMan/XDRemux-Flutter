import XCTest
@testable import XDRemuxCore
@testable import XDremuxAppleFeatures

final class XDRemuxFlutterBackendIOSTests: XCTestCase {
  func testModulesImport() {
    // Phase 1 gate: both vendored modules compile and link for iOS.
    XCTAssertTrue(true)
  }
}
