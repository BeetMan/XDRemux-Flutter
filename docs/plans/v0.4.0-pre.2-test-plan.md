# v0.4.0-pre.2 / pre.3 多平台测试清单

> 相对 pre.1 的增量（38 commits）：Live Photo 配对合成核心、风格嫁接管线
> （universal model graft）、sky matte 去重修复、整理页资产感知 + MOV 避让、
> 批量报告区分、checkpoint 恢复健壮化、linear thumbnail 真生成。
> 测试素材集中在 `../test-media/`（sources/ 真机原件）。

## 0. pre.3 增量：中英文双语 UI

> pre.3 相对 pre.2 只含双语化（`t(zh, en)` 全局层），无功能变化。

- [ ] 设置 → 界面语言切到 English：全部界面文案变英文（主界面 / 设置页 /
  Apple 与 Rust 人像页 / Apple-OPPO 工作流 / 整理页 / 通知与托盘）
- [ ] 英文下切回中文：全部恢复中文
- [ ] 语言切换后队列状态文案（待处理/转换中/已转换/失败等枚举标签）即时刷新
- [ ] 整理页英文下输出目录为 `Still Photos/`、`Live Photos/`（磁盘布局随语言）
- [ ] 英文下批量完成通知与状态栏文案为英文（含「失败 N（其中 M 个格式不支持）」
  对应 `Failed N (M unsupported format)`）
- [ ] 回归一遍：切语言不丢失队列、不触发重新转换

## 一、核心链路（所有平台必测）

### 1. OPPO Motion Photo → Apple 转换（主功能回归）
- [ ] `test-media/sources/oppo-motion-photos/` 五组（4K Live / 标准 1x / 标准 3x /
  大师 1x / 大师 3x）各转一份：成功、无报错
- [ ] `oppo-camera-heic/` 任选 2 张原生 HEIC：转换 + styles 开启，输出可被
  ImageIO 打开（sips 预览）
- [ ] iPhone 上检查：风格 tab 出现、编辑可加载、linear thumbnail 是真实图像
  （不再是黑块）
- [ ] Live Photo 档（配对策略开启）：转换产物 HEIC+MOV 成对；导入 iPhone 后
  实况徽章出现；**确认风格编辑入口关闭（互斥，设计行为）**

### 2. 批量转换与报告（新行为）
- [ ] 混合导入：OPPO 件 + 普通 iPhone 照片（格式不支持）+ 已转换件
- [ ] 完成状态栏应显示 `失败 N（其中 M 个格式不支持）`，而非笼统失败数
- [ ] skipExisting：重复导入已转换件显示「跳过 N 个已转换」

### 3. Checkpoint 恢复（健壮性增量）
- [ ] 批量转换中途杀掉 app → 重开 → 恢复继续
- [ ] 恢复前手动删除某个已转换项的 MOV → 恢复后该项应降级「失败」并重新
  合成（provenance 校验）

### 4. 整理页（资产感知 + MOV 避让，新功能）
- [ ] 扫描含 Live 配对的目录（HEIC+MOV 同名）：归入「实况照片」分组
- [ ] 输出目录已存在同名 `.mov` 时：MOV 复制为 `名字 (2).mov`，不覆盖
- [ ] 原地整理（输入=输出目录）：不误改名、不覆盖

## 二、平台专项

### Windows（x64 安装包 + ARM64 实验包）
- [ ] 安装器安装/卸载干净
- [ ] 拖拽导入（含中文路径、空格路径）
- [ ] 转换性能：4K Live 一张的耗时与 pre.1 对比
- [ ] ARM64 包能启动并完成一次转换（实验级，允许性能差）

### macOS（DMG）
- [ ] DMG 拖拽安装、Gatekeeper 通过
- [ ] AirDrop 回传的 HEIC 直接拖入转换
- [ ] styles 输出在 Photos.app 打开验证（风格 tab + 编辑加载）

### Android（APK）
- [ ] SAF 文件选择器导入（尤其 OEM 文档 provider 路径）
- [ ] 后台批量：前台服务通知进度、锁屏不断
- [ ] 杀进程恢复（checkpoint）在 Android 上专项验证
- [ ] 转换结果自动回存相册（若开启该选项）

### iOS（未签名 IPA，侧载）
- [ ] 侧载工具安装成功
- [ ] 照片库导入/导出权限
- [ ] Live Photo 成对导入（HEIC+MOV）实测配对与实况徽章
- [ ] 风格编辑（identity 状态）打开无报错

## 三、已知设计行为（不是 bug）

- Live Photo 档强制无风格状态（Apple 结构性拦截联合编辑，2026-09-02/03 定论）
- 转换件尾部保留 OPPO Xtra 扩展区（donor canvas 保留决策）
- linear thumbnail 为 8-bit（原生 10-bit，受 x265 MAIN10 构建限制）
- 格式不支持的文件计入失败但单独列出（不计入「按策略跳过」）

## 四、资产

- CI 产物：windows-installer / windows-arm64-installer / android-apk /
  macos-dmg / ios-ipa
- 测试素材：`test-media/sources/`（OPPO 五组 Motion Photo、40+ 相机 HEIC、
  iPhone 3716~3722 风格组、人像组在 `samples/portraits-20260902/`）
